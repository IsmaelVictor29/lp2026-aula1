#include <stdio.h>
int main(){ 
    int nota1;
    int nota2;
    int nota3;
    int total;
        scanf("%d", &nota1);
        scanf("%d", &nota2);
        scanf("%d", &nota3);
        total = (nota1 + nota2 + nota3);
        printf("Pontos Totais do Ano: \n%d",total);
    
       return 0;

}